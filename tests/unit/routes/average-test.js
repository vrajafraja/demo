import { moduleFor, test } from 'ember-qunit';

moduleFor('route:average', 'Unit | Route | average', {
});

test('average route exists', function(assert) {
  let route = this.subject();
  assert.ok(route);
});


